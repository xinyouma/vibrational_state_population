#include <iostream>
#include <string>
#include <vector> 
#include <sstream>
#include <algorithm>
#include <iterator>

//bool readVENUSout(std::vector<std::string> &vecOfStr);
bool readVENUSout(std::vector<std::string> &vecOfStr, unsigned int natom, std::vector<std::vector<float>> &Q, std::vector<std::vector<float>> &P);

